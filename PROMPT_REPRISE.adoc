= Prompt de reprise -- Session 008

__
**EPIC** : Build ISO Xubuntu bootable + installable
**Statut** : Session 007 terminee — Pipeline Gradle + CI/CD en place, ISO jamais buildee
**Mission** : Ecouter les logs CI/CD, debugger le premier build
__

'''

== Session 007 -- Resume

**Date** : 2026-04-26
**Resultat** : OK — Pipeline Gradle ISO complete + CI/CD GitHub Actions

**Connaissances cles** :

* Pipeline Gradle : `./gradlew isoPipeline` (build + verify + test)
* 10 tasks Gradle groupe iso (dockerBuild, isoBuild, isoPurge, isoClean, isoRebuild, isoVerify, isoTestBoot, isoTest, isoPipeline, isoFlash)
* Zero sudo, zero install hote, tout en Docker
* Dockerfile : live-build + qemu-system-x86 + ovmf + genisoimage
* Scripts autodetectent container vs hote (/.dockerenv + /proc/1/cgroup)
* build.sh (hote) → build-inner.sh (container) : un seul docker run
* CI/CD GitHub Actions : Gradle orchestre dockerBuild → isoBuild → isoVerify → isoTestBoot → upload → release
* Ubiquity integre (ubiquity, ubiquity-casper, ubiquity-frontend-gtk, ubiquity-slideshow-xubuntu)
* Menus GRUB UEFI + isolinux BIOS : Try/Install/Test Magic Stick
* Aucune ISO encore generee

**Contraintes EPIC 2** :
* **Zero sudo** : tout en Docker, rien a installer sur l'hote
* **CI/CD GitHub Actions** : orchestre par Gradle, release sur tag v*
* **Publication** : ISO en GitHub Release sur tag v*

**Archive** : `.agents/sessions/007-pipeline-gradle-iso.adoc`

'''

== Session 008 -- Priorites

**Mission** : Ecouter les logs CI/CD, debugger le premier build

=== 1. Lancer le build en local

* `./gradlew isoPipeline`
* Analyser les logs en temps reel
* Duree : 30-60 min

=== 2. Analyser les logs CI/CD GitHub Actions

* Pousser un commit pour declencher le workflow
* Suivre les logs du workflow build.yml
* Identifier les erreurs et les corriger
* Iterer jusqu'a ce que la pipeline passe

=== 3. Debugger si necessaire

* Build echoue : analyser build/logs/, corriger configs live-build
* Boot echoue : ajuster GRUB/isolinux, bootappend-live, casper
* CI/CD echoue : ajuster .github/workflows/build.yml

=== Criteres d'Acceptation

* [ ] `./gradlew isoBuild` genere l'ISO avec succes en local
* [ ] `./gradlew isoVerify` OK
* [ ] `./gradlew isoTestBoot` OK (kernel boote BIOS + UEFI)
* [ ] CI/CD GitHub Actions passe (vert)

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo** : tout en Docker, orchestre par Gradle
* Archive + PROMPT_REPRISE mis a jour en fin de session