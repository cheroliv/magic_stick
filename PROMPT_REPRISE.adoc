= Prompt de reprise -- Session 012

__
**EPIC** : Build ISO Xubuntu bootable + installable
**Statut** : Session 011 interrompue — QEMU bloque sur initramfs
**Mission** : Diagnostiquer et corriger le blocage initramfs, puis UEFI + EPIC 3
__

'''

== Session 011 -- Resume

**Date** : 25 avril 2026
**Resultat** : Bloque — QEMU boote le kernel mais s'arrete sur `(initramfs)` sans message d'erreur

**Probleme** : casper ne trouve probablement pas `filesystem.squashfs` dans le peripherique de boot. Aucun correctif applique.

**Archive** : `.agents/sessions/011-qemu-initramfs-debug.adoc`

'''

== Session 012 -- Priorites

**Mission** : Diagnostiquer le blocage initramfs, corriger, puis avancer UEFI et EPIC 3

=== 1. Diagnostiquer le blocage initramfs (PRIORITE MAXIMALE)

* Relancer QEMU avec `-serial stdio` pour capturer les messages kernel
* Ajouter `break=premount` dans les parametres kernel pour shell initramfs
* Verifier que `filesystem.squashfs` est dans l'ISO : `isoinfo -l -i binary.hybrid.iso | grep squashfs`
* Verifiez les parametres kernel dans isolinux.cfg (boot=casper, find_iso, etc.)
* Tester avec `-cdrom` au lieu de `-drive` pour l'emulation CD

=== 2. Apres correction : Ajouter support UEFI

* Creer hook `999-add-efi-support.binary`
* Utiliser grub-mkimage pour creer l'image EFI
* Ajouter EFI a l'ISO via xorriso
* Tester boot UEFI avec QEMU

=== 3. Commencer EPIC 3 : Partition A/B + Persistence

* Lire `AB_PARTITION_ESSENTIALS.adoc` et `AB_PARTITION_REFERENCE.adoc`
* Creer le script `setup-ab.sh`
* Tester la persistence (casper-rw/overlay)

=== Criteres d'Acceptation

* [ ] ISO bootable en BIOS (QEMU OK)
* [ ] ISO bootable en UEFI
* [ ] Setup A/B partition fonctionnel
* [ ] Persistence validee

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo** : tout en Docker, orchestre par Gradle
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* Archive + PROMPT_REPRISE mis a jour en fin de session