= Prompt de reprise — Session 042

__
**EPIC** : EPIC 2 (valider build v0.1.7) + EPIC 9 (VS Code/gh dans /opt/)
**Statut** : Session 041 terminee — fixes flatpak + JetBrains deployes, fix ffmpeg (apt-mark) non pousse
__

'''

== Session 041 — Résumé

- Fix flatpak : remis dans `tools.list.chroot` (run #25084062829)
- Fix JetBrains Toolbox : symlink `bin/jetbrains-toolbox` (run #25085436100)
- Nouveau FAIL : ffmpeg supprime par `apt-get autoremove`
- Fix ffmpeg : `apt-mark manual ffmpeg flatpak` dans `080-cleanup-space.chroot`

== Session 042 — Priorites

=== 1. Relancer workflow avec fix ffmpeg

* Commit/push le fix `080-cleanup-space.chroot` (apt-mark manual)
* `gh workflow run build.yml`, attendre vert complet

=== 2. Si VERT complet → Bump v0.1.7 + release

* Modifier `VERSION` → `0.1.7`, `git tag v0.1.7 && git push origin v0.1.7`
* Monitorer : upload artifact + SourceForge + Docker Hub

=== 3. EPIC 9 — VS Code + gh CLI dans /opt/

* VS Code : tarball dans `/opt/VSCode-linux-x64/` + script update
* gh CLI : binaire GitHub Releases dans `/opt/gh/` + symlink
* Ajouter checks dans `test-software.sh`, valider CI/CD

=== 4. EPIC 5 (si temps) — Polishment XFCE

* Verifier menu XFCE apres boot QEMU

== Regles

* Jamais de commit sans permission
* Racine de travail = magic_stick/
* Zero sudo, zero build local (CI/CD uniquement)
* INTERDICTION DE LOG LES TOKENS
* FICHIERS PERSONNELS : NE JAMAIS modifier ~/.*
