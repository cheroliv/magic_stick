= Prompt de reprise -- Session 010

__
**EPIC** : Build ISO Xubuntu bootable + installable
**Statut** : Session 009 terminee — Cause racine cpio identifiee, correctifs appliques (non pouses)
**Mission** : Pousser les correctifs, obtenir la premiere ISO
__

'''

== Session 009 -- Resume

**Date** : 2026-04-24
**Resultat** : OK — Cause racine identifiee et corrigee, en attente de push

**Cause racine cpio premature end of archive** :
* `--bootloader grub-efi` non gere par les scripts `lb_binary_*` de live-build
* `gfxboot-theme-ubuntu` absent d'Ubuntu 24.04 → bootlogo cpio vide → erreur
* `boot=live` au lieu de `boot=casper` (incompatibilite)

**Correctifs appliques** (en local, non commit) :
* `--bootloader grub-efi` → `--bootloader syslinux`
* Ajout `--syslinux-theme live-build` + `--initramfs casper`
* `boot=live config` → `boot=casper`
* `syslinux` + `grub-efi-amd64-signed` dans Dockerfile
* Suppression includes.binary/ custom (isolinux.cfg, grub.cfg)

**Archive** : `.agents/sessions/009-cpio-debug-iso-build.adoc`

'''

== Session 010 -- Priorites

**Mission** : Obtenir la premiere ISO generee avec succes

=== 1. Commit + push les correctifs

* Commit les 3 fichiers modifies (build-inner.sh, Dockerfile, includes.binary/)
* Pousser sur main → declencher CI/CD
* Watch le run GitHub Actions

=== 2. Iterer CI/CD jusqu'a build vert

* Si echec : analyser les logs (`gh run view <id> --log-failed`)
* Corriger, commit, push, watch
* Objectif : ISO generee + verify OK

=== 3. Si build passe

* Telecharger l'ISO artifact
* Tester en local avec QEMU
* Celebrer

=== 4. Ajouter support UEFI (apres premiere ISO)

* Creer hook binaire `999-add-efi-support.binary`
* Utiliser grub-mkimage pour creer l'image EFI
* Ajouter EFI image a l'ISO via xorriso

=== Criteres d'Acceptation

* [ ] `./gradlew isoBuild` genere l'ISO avec succes en CI/CD
* [ ] `./gradlew isoVerify` OK en CI/CD
* [ ] `./gradlew isoTestBoot` OK en CI/CD
* [ ] CI/CD GitHub Actions vert

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo** : tout en Docker, orchestre par Gradle
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* Archive + PROMPT_REPRISE mis a jour en fin de session