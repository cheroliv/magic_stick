= Prompt de reprise -- Session 019

__
**EPIC** : Correction Docker + Validation tests boot + A/B CI/CD
**Statut** : Session 018 terminee — Docker VNC OK, scripts A/B crees, CI/CD mis a jour
**Mission** : Corriger le Docker ENTRYPOINT + valider test-boot.sh --vnc + test-ab-partition.sh en CI/CD
__

'''

== Session 018 -- Resume

**Date** : 26 avril 2026
**Resultat** : Docker image reconstruite avec VNC (novnc, tigervnc, websockify, x11vnc), test-boot.sh mode --vnc, test-ab-partition.sh complet, update-system.sh --yes pour CI/CD, taches Gradle + workflow GA ajoutes

**Archive** : `.agents/sessions/018-docker-vnc-ab-partition-ci.adoc`

'''

== Session 019 -- Priorites

**Mission** : Corriger les regresions Docker et valider les tests en conteneur

=== 1. Corriger le Dockerfile (regression ENTRYPOINT)

* Remplacer `WORKDIR /magic_stick` par `CMD ["bash"]` (ENTRYPOINT=bash invalide les commandes docker run directes)
* Verifier QUE les taches Gradle existantes (`isoBuild`, `isoVerify`, `isoTestBoot`, `isoFlash`) fonctionnent encore
* Tester `docker run --rm magic_stick:builder /magic_stick/scripts/test-boot.sh --vnc ...`

=== 2. Valider test-boot.sh --vnc

* Lancer test-boot.sh --vnc sur ISO complete (attendre build CI/CD ou re-telcharger ISO)
* Valider QEMU VNC server + websockify + noVNC dans le conteneur
* Verifier que localhost:6080/vnc.html est accessible depuis le navigateur
* Tester boot BIOS + UEFI explicitement (--bios et --uefi)

=== 3. Valider test-ab-partition.sh en CI/CD

* Lancer isoTestAB localement en conteneur privileged + loop devices
* Corriger les erreurs eventuelles (loop device, partprobe, mkfs)
* Executer en CI/CD GitHub Actions (privileged runners)
* Documenter les resultats dans SCRIPT_VERIFICATION.adoc

=== 4. Mettre a jour la documentation

* Mettre a jour BOOT_TEST_REFERENCE.adoc avec mode VNC + noVNC
* Documenter test-ab-partition.sh dans le site (ab-partition.adoc)
* Mettre a jour INDEX.adoc (session 018 dans recentes)

=== Priorites reportees

* EPIC 7 (Auto-update doc + IA release notes) : reporte
* EPIC 4 (Logiciels Ollama/Docker/SDKMAN) : depend du boot
* EPIC 5 (Customisation XFCE) : cosmetique

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo sur l'hote** : tout via CI/CD ou conteneur Docker
* **INTERDICTION DE LOG LES TOKENS** — cf AGENT.adoc
* **Zero build local** : GitHub Actions uniquement, lire les logs
* **SourceForge** : rsync via SSH, pas d'API S3
* **Boot test** : QEMU+VNC uniquement (cf `.agents/BOOT_TEST_REFERENCE.adoc`)
* **A/B test** : loopback device en Docker privileged uniquement (cf `.agents/AB_PARTITION_REFERENCE.adoc`)
