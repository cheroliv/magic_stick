= Prompt de reprise -- Session 007

__
**EPIC** : Build ISO Xubuntu bootable + installable
**Statut** : Session 007 en cours -- Pipeline Gradle + CI/CD OK, pret pour build
**Mission** : Builder la premiere ISO via `./gradlew isoPipeline`
__

'''

== Session 007 -- Etat Actuel

**Date** : 2026-04-26
**Resultat partiel** : Pipeline Gradle ISO complete + CI/CD GitHub Actions

**Connaissances cles** :

* Pipeline Gradle orchestree : `./gradlew isoPipeline` (build + verify + test)
* Tasks Gradle : dockerBuild, isoBuild, isoPurge, isoClean, isoVerify, isoTestBoot, isoTest, isoPipeline, isoRebuild, isoFlash
* Tout en Docker : zero sudo, zero install sur l'hote
* Dockerfile : live-build + qemu-system-x86 + ovmf + genisoimage
* Scripts verify.sh et test-boot.sh : autodetectent container vs hote, lancent Docker si necessaire
* build.sh (hote) → build-inner.sh (container) : un seul docker run
* CI/CD GitHub Actions : checkout + JDK + Gradle → `./gradlew dockerBuild isoBuild isoVerify isoTestBoot`
* build.gradle.kts : tasks bakery+a11y (docs site) + tasks iso (pipeline build)
* Ubiquity integre : ubiquity, ubiquity-casper, ubiquity-frontend-gtk, ubiquity-slideshow-xubuntu
* Menus GRUB UEFI + isolinux BIOS : Try/Install/Test Magic Stick

**Contraintes EPIC 2** :
* **Zero sudo** : tout en Docker, rien a installer sur l'hote
* **CI/CD GitHub Actions** : orchestre par Gradle, release sur tag v*
* **Publication** : ISO en GitHub Release sur tag v*

'''

== Session 008++ -- Priorites

**Mission** : Builder la premiere ISO via Gradle

=== 1. Lancer le build

* `./gradlew isoPipeline`
* Verifier qu'il n'y a pas d'erreur dans la pipeline Gradle
* Duree : 30-60 min

=== 2. Debugger si necessaire

* Si build echoue : analyser logs dans build/logs/
* Si boot echoue : ajuster GRUB/isolinux, bootappend-live, casper

=== Criteres d'Acceptation

* [ ] `./gradlew isoBuild` genere l'ISO avec succes
* [ ] `./gradlew isoVerify` OK
* [ ] `./gradlew isoTestBoot` OK (kernel boote BIOS + UEFI)
* [ ] CI/CD GitHub Actions pret

'''

== Regles

* Jamais de commit sans permission
* Pas de commandes destructrices sans permission
* Racine de travail = `magic_stick/`
* **Zero sudo** : tout en Docker, orchestre par Gradle
* Archive + PROMPT_REPRISE mis a jour en fin de session