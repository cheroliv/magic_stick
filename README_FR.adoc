= Magic Stick

_Environnement portable standardise sur cle USB bootable_

image:https://img.shields.io/badge/version-0.1.6-blue[Version 0.1.6]
image:https://img.shields.io/badge/base-Xubuntu%2024.04-orange[Base Xubuntu 24.04]
image:https://img.shields.io/badge/licence-Apache%202.0-green[Licence Apache 2.0]

**image:https://img.shields.io/badge/⬇_Telecharger_ISO-v0.1.6-brightgreen[Telecharger v0.1.6]**

* https://sourceforge.net/projects/magic-stick/files/[SourceForge] — ISO brute

IMPORTANT: Flasher l'ISO directement :
[source,bash]
----
sudo dd if=magic_stick_0.1.6.iso of=/dev/sdX bs=4M status=progress && sync
----

'''

== Pourquoi Magic Stick ?

Vous etes technicien terrain, formateur ou dev nomade. A chaque intervention, vous perdez 30 minutes a configurer l'ordinateur du moment. Magic Stick elimine ce probleme : **une cle USB qui boote un environnement complet et identique sur n'importe quel PC**.

* **Formation** : chaque apprenant a la meme cle, pret a bosser des le premier jour
* **Technicien FTTH/reseau** : diagnostique reseau complet quel que soit l'ordi
* **Dev nomade** : retrouver sa stack (Docker, Java, Ollama) sur n'importe quelle machine
* **Equipe technique** : environnement standardise partage, zero temps perdu en config

'''

== Comment ca marche ?

Magic Stick utilise un systeme de **partition A/B** avec persistence :

[cols="1,2,3"]
|===
| Partition | Taille | Role

| System A
| ~8 Go
| Systeme live Xubuntu actif (boote par defaut)

| System B
| ~8 Go
| Systeme live Xubuntu de secours (inactif)

| Persistence
| Reste de la cle
| Donnees utilisateur + config logiciels
|===

=== Mise a jour atomique

1. Le script `update-system.sh` detecte la partition active (A ou B)
2. Il ecrit la nouvelle ISO sur la partition **inactive**
3. Il inverse le flag de boot GRUB
4. Au prochain demarrage : nouvelle version
5. Si ca plante ? Reboot sur l'ancienne partition — **rollback instantane**
6. La partition Persistence n'est **jamais** touchee

'''

== Logiciels inclus

[cols="2,3"]
|===
| Categorie | Logiciels

| Environnement
| Xubuntu Desktop (XFCE)

| Conteneurisation
| Docker CE + Docker Compose, Podman

| IA locale
| Ollama (LLM sur la machine)

| Developpement
| SDKMAN + JDK 25 Temurin, Git, curl, wget, jq, JetBrains Toolbox

| JavaScript/Node
| NVM, Node.js, pnpm

| Python
| Python 3.14.4, uv package manager

| Reseau/FTTH
| nmap, iperf3, Wireshark, tshark, traceroute, dnsutils

| Diagnostic systeme
| htop, ncdu, tree, lshw, smartmontools

| Recherche/Navigation
| ripgrep, fd, fzf

| Outils Git
| lazygit

| HTTP/REST
| xh, httpie

| Task runner
| just

| Gestionnaire de paquets
| Flatpak

| Shell
| zsh + oh-my-zsh, starship

| Media
| opencode, graphify, ffmpeg

| SSH/Transfert
| OpenSSH client, rsync, tmux, terminator
|===

'''

== Demarrage rapide

=== Prerequis

* Une cle USB de 32 Go minimum (64 Go recommande)

=== Telecharger l'ISO

* https://sourceforge.net/projects/magic-stick/files/[SourceForge] — ISO brute

NOTE: L'ISO est construite par GitHub Actions sur chaque push de tag. Pas de build local necessaire.

=== Flasher sur une cle USB

[source,bash]
----
# Identifier la cle USB
lsblk

# Flasher (ATTENTION : efface toutes les donnees du peripherique)
sudo scripts/flash.sh /dev/sdX
----

=== Mettre a jour le systeme (A/B)

[source,bash]
----
# Mettre a jour la partition inactive
sudo scripts/update-system.sh /dev/sdX

# Si le nouveau systeme ne boot pas :
# selectionner l'ancienne partition dans le menu GRUB
----

'''

== Architecture du projet

[source]
----
magic_stick/
├── build/                             # Sortie live-build (ISO generees)
├── config/
│   ├── live-build/
│   │   ├── package-lists/            # Listes de paquets
│   │   ├── hooks/                     # Hooks chroot + binary
│   │   ├── includes.chroot/          # Fichiers inclus dans le systeme
│   │   └── includes.binary/          # Fichiers inclus dans l'ISO
│   └── overrides/
│       └── skel/                      # /etc/skel customise (modele home)
├── scripts/
│   ├── build.sh                       # Build d'ISO via Docker (point d'entree)
│   ├── build-inner.sh                 # Build d'ISO (dans le conteneur Docker)
│   ├── flash.sh                       # Flash USB
│   ├── test-boot.sh                   # Test de boot QEMU
│   ├── update-system.sh               # Mise a jour A/B
│   ├── install-software.sh            # Logiciels dans le chroot
│   └── verify.sh                      # Verification de l'ISO
├── src/
│   └── main/resources/                # Ressources de build Gradle
├── Dockerfile                         # Image Docker pour le build
├── build.gradle.kts                   # Config de build Gradle
└── .github/workflows/build.yml        # Pipeline CI/CD
----

'''

== Partitionnement recommande (cle 64 Go)

|===
| Partition | Taille | Type | Flag
| 1 (System A) | 8 Go | squashfs | boot
| 2 (System B) | 8 Go | squashfs | -
| 3 (Persistence) | ~48 Go | ext4 | -
|===

La partition Persistence utilise `persistence.conf` avec `/ union` pour sauvegarder les donnees entre les sessions.

'''

== Securite

* **Aucune donnee personnelle** dans l'ISO — les cles SSH et fichiers utilisateur sont dans la partition Persistence
* **Rollback automatique** si le nouveau systeme ne boot pas
* **Donnes persistantes jamais touchees** par les mises a jour systeme
* Mises a jour atomiques : si ca casse, on revient a la version precedente en un reboot

'''

== Etat du projet

|===
| EPIC | Statut

| EPIC 1 : Initialisation projet
| ✅ Termine

| EPIC 2 : Build ISO Xubuntu de base
| ✅ Termine — https://sourceforge.net/projects/magic-stick/files/[Telecharger v0.1.6]

| EPIC 3 : Partition A/B + Persistence
| ✅ Termine

| EPIC 4 : Logiciels (Ollama, Docker, SDKMAN, outils)
| ✅ Termine

| EPIC 5 : Personnalisation XFCE
| 🔄 En cours

| EPIC 6 : Script de mise a jour A/B
| ✅ Termine
|===

'''

== Licence

Apache License 2.0